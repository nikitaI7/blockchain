1 clone files to directory
2 create file .env
3 write in .env
    TELEGA_TOKEN = 'Token' //create token in telegram https://t.me/BotFather
    api_key = 'key' //take api key there :
    name = 'blockchainbot'
    user = 'blockchainbot'
    password = 'blockchainbot123'
    db_host = '127.0.0.1'
4 open them in terminal
5 in  terminal write:
6 docker-compose up --build database
7 docker-compose up --build create_db
8 docker-compose up --build bot_in bot_out database
